<!DOCTYPE html>
<html lang="kr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= community.name %> - 커뮤니티 상세정보</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' href='/stylesheets/community.css' />
    <link rel='stylesheet' href='/stylesheets/detail.css' />
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-transparent fixed-top">
    <div class="container">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto" id="menu">
                <li class="nav-item">
                    <a class="nav-link" href="/index">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Community">Community</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#services">Services</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#contact">Contact</a>
                </li>
            </ul>
        </div>
        <div id="user-info" class="align-items-center mt-4" style="display: flex;">
            <span id="user-name" class="h5 text-white mt-1" style="display: flex;">Username</span>
            <button id="logout-btn" class="btn btn-danger btn-sm" style="display: flex;">로그아웃</button>
        </div>
    </div>
</nav>
<div class="container mt-5">
    <div class="card shadow-sm p-4 mb-4">
        <h2 class="mb-4 text-primary"><%= community.name %></h2>
        <p><strong>설명:</strong> <%= community.description %></p>
        <p><strong>위치:</strong> <%= community.location %></p>
        <p><strong>생성자:</strong> <%= community.created_by %></p>
        <p><strong>생성일:</strong> <%= community.created_at.toDateString() %></p>
        <p><strong>마지막 수정일:</strong> <%= community.updated_at ? community.updated_at.toDateString() : 'N/A' %></p>
        <a href="/community/<%= community.community_id %>/post/new" class="btn new-post-btn mt-4">새 포스트 작성</a>
    </div>

    <div class="card shadow-sm p-4">
        <h3 class="mb-4" id="postlist">포스트 목록</h3>
        <% if (posts.length === 0) { %>
            <p>이 커뮤니티에 작성된 포스트가 없습니다.</p>
        <% } else { %>
            <ul class="list-group">
                <% posts.forEach(function(post) { %>
                    <li class="list-group-item">
                        <h5><a href="/post/<%= post.post_id %>" class="post-title"><%= post.title %></a></h5>
                        <p><small>작성자: <%= post.author %> | 작성일: <%= post.created_at.toDateString() %></small></p>
                        <% if (post.image_url) { %>
                            <img src="<%= post.image_url %>" alt="이미지" class="img-fluid">
                        <% } %>
                    </li>
                <% }); %>
            </ul>
        <% } %>
    </div>
</div>
<script src='/javascripts/background.js'></script>
<script src='/javascripts/community.js'></script>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
